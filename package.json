{
  "name": "stage-a2-avis-juridiques",
  "version": "1.0.0",
  "description": "Gestion des Avis Juridiques — application PERN (Natixis DAJ)",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "db:schema": "psql -U postgres -d avis_juridiques -f database/schema.sql",
    "db:seed": "psql -U postgres -d avis_juridiques -f database/seed.sql",
    "db:reset": "npm run db:schema && npm run db:seed"
  },
  "author": "BOUKERMA Oussama",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
